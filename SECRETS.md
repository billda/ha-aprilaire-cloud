# Test credentials

Tests must use synthetic account, user, device, and location identifiers.
Never commit AprilAire account credentials or identifiers copied from a real
account. Review staged changes and run
`gitleaks git --pre-commit --redact --staged` before committing.
